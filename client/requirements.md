## Packages
lucide-react | Icons for the UI
date-fns | Formatting dates for exams and attempts

## Notes
No additional backend assumptions.
Timer relies on `durationMinutes` fetched from the exam details. Auto-submission is handled on the client when the timer reaches 0.
